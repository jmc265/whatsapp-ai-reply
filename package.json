{
  "name": "whatsapp-ai-reply",
  "version": "0.0.1",
  "description": "WhatsApp bot that uses AI to help with replies",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Cross",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
